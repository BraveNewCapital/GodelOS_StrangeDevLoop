# Test-only dependencies for GödelOS
# Pin pytest to a 7.x release to avoid ImportError around internal types (e.g., FixtureDef)
# that some plugins expect and which changed in pytest 8.x.
pytest==7.4.4

# Async test utilities compatible with pytest 7.x
pytest-asyncio==0.21.1
# Lifespan manager for ASGI apps used in tests
asgi-lifespan==2.1.0
